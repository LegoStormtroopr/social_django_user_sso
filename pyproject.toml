[project]
name = "social-django-user-sso"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samuel Spencer", email = "theodore.therone@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
    "social-auth-app-django>=6.0.1",
]

[project.scripts]
social-django-user-sso = "social_django_user_sso:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
